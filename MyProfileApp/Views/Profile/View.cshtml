@model MyProfileApp.ViewModels.ViewProfileViewModel
@{
    ViewBag.Title = "View";
}

<div>
    <h2>@Model.Person.Name</h2>

    @foreach (var s in Model.Skills)
    {
        <h4>@s.Name</h4>
    }
</div>
